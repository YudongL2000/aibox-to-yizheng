# Folder Plan: backend/src/config

<!-- vibe-folder-map:start -->
## Architecture
- Scope: backend/src/config
- Function: Application logic and reusable modules.
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `n8n-api.ts` | `module` | Provide n8n api capability for backend/src/config, preferring embedded localhost runtime credentials over stale local `.env` values when Electron rotates ports |
<!-- vibe-folder-map:end -->
