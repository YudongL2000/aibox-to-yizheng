# Folder Plan: frontend/lib/module/video_stream

<!-- vibe-folder-map:start -->
## Architecture
- Scope: frontend/lib/module/video_stream
- Function: 视频流模块，负责 MQTT 驱动的推拉流页，并保持状态文案最小化。
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `model/` | `dir` | Subfolder anchor for model under frontend/lib/module/video_stream |
| `video_stream_page.dart` | `module` | 视频推拉流页，保留连接/推流/等待等必要状态词，去掉额外说明 |

Theme hardening note:
- `video_stream_page.dart` 已确认不含 UI 层硬编码色值，继续按 `spatial` 语义 token 渲染。
<!-- vibe-folder-map:end -->
