# Folder Plan: frontend/lib

<!-- vibe-folder-map:start -->
## Architecture
- Scope: frontend/lib
- Function: Local module responsibilities for frontend/lib.
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `main.dart` | `entry` | Provide main capability for frontend/lib |
| `module/` | `dir` | Subfolder anchor for module under frontend/lib |
| `server/` | `dir` | Subfolder anchor for server under frontend/lib |
| `utils/` | `dir` | Subfolder anchor for utils under frontend/lib |
| `webView/` | `dir` | Subfolder anchor for webView under frontend/lib |

Theme hardening note:
- `main.dart` keeps a forced dark Material theme entry and is now validated against hardcode/style scanning rules.
- `utils/spatial_design_ref.dart` is the single source for Flutter workbench radii; workspace pages should default to low-radius rectangular shells instead of ad-hoc pills or `+10` card corners.
- `utils/spatial_design_ref.dart` also owns the shared dark-shell background hex mirrored into the web viewer fallback; `Color` and hex constants must stay in sync.
<!-- vibe-folder-map:end -->
