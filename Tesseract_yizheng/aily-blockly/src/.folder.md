# Folder Plan: aily-blockly/src

<!-- vibe-folder-map:start -->
## Architecture
- Scope: aily-blockly/src
- Function: Application logic and reusable modules.
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `app/` | `dir` | Subfolder anchor for app under aily-blockly/src |
| `index.html` | `entry` | Provide index capability for aily-blockly/src |
| `main.ts` | `entry` | Provide main capability for aily-blockly/src |
| `spatial-design-ref.scss` | `module` | Provide Spatial Wireframe global token and utility reference for aily-blockly/src |
| `styles.scss` | `module` | Provide styles capability for aily-blockly/src |

Theme hardening note:
- `index.html` startup 期间颜色从 literal hex 迁移至 `spatial-design-ref.scss` CSS 变量表达。
- `styles.scss` 负责把 `spatial-design-ref.scss` 的 wireframe token 扩散到 ng-zorro 按钮、输入框、tabs、弹层与全局 shell 控件，避免 Angular 组件各自维护第二套视觉系统。
<!-- vibe-folder-map:end -->
