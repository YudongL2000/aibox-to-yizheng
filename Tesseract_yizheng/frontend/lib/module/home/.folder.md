# Folder Plan: frontend/lib/module/home

<!-- vibe-folder-map:start -->
## Architecture
- Scope: frontend/lib/module/home
- Function: 首页与工作台模块，负责纯数字孪生入口、顶部 tabs 的 Workflow/Digital Twin 工作台，以及相关控制器、组件装配与精简后的界面文案。
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `AGENTS.md` | `doc` | Provide AGENTS capability for frontend/lib/module/home |
| `bloc/` | `dir` | Subfolder anchor for bloc under frontend/lib/module/home |
| `controller/` | `dir` | Subfolder anchor for controller under frontend/lib/module/home |
| `home_instance.dart` | `module` | Provide home instance capability for frontend/lib/module/home |
| `home_main_page.dart` | `module` | 首页入口壳，保留 prompt 输入与导航动作，去掉装饰性说明文案 |
| `home_market_page.dart` | `module` | Provide home market page capability for frontend/lib/module/home |
| `home_my_product_page.dart` | `module` | 产品展示页，保留作品/市场卡片，去掉额外说明与页脚噪音 |
| `home_workspace_page.dart` | `module` | 顶层工作台页，负责纯数字孪生入口与顶部 tabs 的 Workflow/Digital Twin 主视图切换，并收敛多余辅助说明 |
| `model/` | `dir` | Subfolder anchor for model under frontend/lib/module/home |
| `provider/` | `dir` | Subfolder anchor for provider under frontend/lib/module/home |
| `widget/` | `dir` | Subfolder anchor for widget under frontend/lib/module/home |

近期同步:
- `home_main_page.dart` 已修复宽窄布局变量（`isWide`），并继续保持 Token 化颜色入口。
- `home_workspace_page.dart` 已把 embedded Digital Twin 与 tabbed workspace 的页面基底收敛到 workflow 同一套底色/强调色，避免主视图切换时出现两套 page palette。
<!-- vibe-folder-map:end -->
