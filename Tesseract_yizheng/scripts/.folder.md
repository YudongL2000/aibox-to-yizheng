# Folder Plan: scripts

<!-- vibe-folder-map:start -->
## Architecture
- Scope: scripts
- Function: Shell automation and guard checks for workspace consistency.
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `docs-check.sh` | `script` | Guard script that checks doc references and folder contracts |
| `pre-commit-folder-check.sh` | `script` | Guard script that enforces folder contract presence before commit |
| `check-no-hardcodes.mjs` | `script` | Hardcode-style guard for Flutter + aily-blockly UI token migration |

Recent hardcode migration sync:
- `check-no-hardcodes.mjs` now supports `--module=aily-blockly` as the canonical aily-blockly module name and scans `index.html` as well as TS/SCSS render layers.
<!-- vibe-folder-map:end -->
