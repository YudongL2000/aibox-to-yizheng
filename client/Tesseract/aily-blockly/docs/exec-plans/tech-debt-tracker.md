# Tech Debt Tracker

## 开放债务

| 债务 | 现象 | 影响 | 下一步 |
| --- | --- | --- | --- |
| 跨仓构建依赖重 | 桌面 runtime 依赖兄弟目录 `backend` 与 `n8n` 的构建产物 | 新环境启动成本高，失败点分散 | 收敛 prepare 脚本输出，明确一键前置检查 |
| 双编辑器模式复杂 | Tesseract Studio 与 legacy Blockly 共存 | 项目模式判断、侧栏分支和测试矩阵增大 | 持续把新能力只落在 Tesseract 主路径，减少重复实现 |
| 嵌入式 n8n 环境敏感 | 本地端口、数据目录、可选 Python 任务运行器都会影响体验 | 启动成功率依赖机器状态 | 固化 runtime 健康检查与更明确的错误提示 |
| 安全文档与实现可能漂移 | 路径/命令策略已文档化，但未来实现改动可能先发生 | 文档失真会直接误导 AI 和维护者 | 每次安全策略改动同步更新 `docs/SECURITY.md` 与相关服务注释 |

## 已处理债务
- 根目录零散的 aily-blockly 方案文档已经收敛进当前 `docs/` 树，避免继续和实现脱节。
