# contracts/
> L2 | 父级: /mnt/c/Users/sam/Documents/Sam/code/Tesseract/specs/004-workflow-view-sync/AGENTS.md

成员清单
workflow-view-sync-contract.md: 工作流视图同步契约，定义创建成功、占位态与嵌入式工作区切换之间的状态边界。

法则
- 契约文档只描述跨边界输入输出，不承载模块内部实现细节。
- 所有字段都必须服务于“后端 workflow 真相如何驱动客户端画布”这条主线，禁止再次长出浏览器优先或主页回退的第二套语义。

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
