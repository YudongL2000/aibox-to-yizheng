# Component Reuse Rule

- Prefer reusing existing components/modules before creating new ones.
- Search current workspace for semantically similar implementation first.
- New component/module creation is allowed only when reuse cannot satisfy requirement.
- Record reuse decision in related `.folder.md`.
