# Session Start

## Start Checklist

1. 读取根级 `CLAUDE.md`，确认加载入口和全局规则。
2. 读取根级 `.folder.md`，确认本次涉及的模块边界。
3. 进入目标模块前，先读该模块的 `.folder.md` 和 `AGENTS.md`。
4. 涉及行为变更时，补看对应 `specs/<feature>/` 下的 spec、checklist、contracts。
5. 修改完成后，同步更新受影响目录的 `.folder.md`，并运行最小必要验证。
