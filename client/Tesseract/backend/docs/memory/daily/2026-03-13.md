# 2026-03-13

## New Facts

- 在 `docs/` 下建立了 `memory/` 目录，作为长时程项目上下文层。
- 记忆结构采用 `MEMORY.md + daily/ + bank/ + bank/entities/` 分层。
- 当前长周期开发主线是 `Refactor-4 / Harness Engineering`。
- 补充了 `OPERATING_RULES.md` 与 `daily/TEMPLATE.md`，把使用方式和每日格式固定下来。
- 新增一个可迁移 Skill，并将其提升到仓库顶层 `skills/`，用于在其他项目一次性初始化同类 memory 体系。

## Decisions

- 所有新信息先落在 `daily/`，不直接写入 `MEMORY.md`。
- `MEMORY.md` 只保留跨会话始终需要的小核心。
- `bank/entities/` 只为反复出现的项目/产品/人物建页。
- 后续其他项目如果要初始化同类体系，优先复用 Skill，而不是手工重新搭目录。

## Constraints

- 不保存整段 transcript。
- 不保存密钥、token、密码。
- 架构级目录调整必须同步 `CLAUDE.md`。

## Retain

- `memory/` 现在是长时程开发上下文的正式入口。
- `Refactor-4` 是当前最重要的长期架构主题。
- 新知识先写 `daily/`，稳定后再晋升到 `bank/` 或 `MEMORY.md`。
- `OPERATING_RULES.md + daily/TEMPLATE.md + Skill` 组成了这套 memory 体系的最小可迁移闭环。
